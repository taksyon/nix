# --- extra Hyprland config only on Yoga 7 (because only yoga7 imports this) ---
# bind = Super, Return, exec, foot
bind = Super, B, exec, firefox

gesture = $workspaceSwipeFingers, pinch, fullscreen

exec-once = iio-hyprland
exec-once = sleep 1s; hyprctl dispatch exec \[workspace 1 silent\; tile\] foot
exec-once = sleep 2s; hyprctl dispatch exec \[workspace 2 silent\; tile\] firefox 

plugin {
    touch_gestures {
        sensitivity = 1.0
        workspace_swipe_fingers = 3
        workspace_swipe_edge = d
        long_press_delay = 400
        resize_on_border_long_press = true
        edge_margin = 10
        emulate_touchpad_swipe = false
                # swipe left from right edge
        hyprgrass-bind = , edge:r:l, workspace, +1

        # swipe up from bottom edge
        hyprgrass-bind = , edge:d:u, exec, firefox

        # swipe down from left edge
        hyprgrass-bind = , edge:l:d, exec, pactl set-sink-volume @DEFAULT_SINK@ -4%

        # swipe down with 4 fingers
        hyprgrass-bind = , swipe:4:d, killactive

        # swipe diagonally left and down with 3 fingers
        # l (or r) must come before d and u
        hyprgrass-bind = , swipe:3:ld, exec, foot

        # tap with 3 fingers
        hyprgrass-bind = , tap:3, exec, foot

        # pinch in with 3 fingers
        hyprgrass-bind = , pinch:3:i, exec, foot

        # longpress can trigger mouse binds:
        hyprgrass-bindm = , longpress:2, movewindow
        hyprgrass-bindm = , longpress:3, resizewindow
    }
}
